# backend/requirements.txt


fastapi==0.115.0
uvicorn[standard]==0.30.0
httpcore>=1.0.0  # Loosened: Allows updates while keeping 1.0.x series for compatibility
httpx>=0.26.0  # Updated: Resolves conflict with supabase; will pull ~0.27.x or 0.28.x latest
supabase>=2.18.1  # Unchanged: Will now install 2.24.0 (latest stable)
# gotrue>=2.7.0  # Still removed: Deprecated and unnecessary
pydantic>=2.9.2
pydantic-core>=2.23.0
pydantic-settings==2.1.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6
python-dotenv==1.0.0
email-validator==2.1.0


passlib[bcrypt]==1.7.4
bcrypt<4.0.0  # New: Pins to 3.2.2 (latest 3.x); resolves __about__ without security loss
